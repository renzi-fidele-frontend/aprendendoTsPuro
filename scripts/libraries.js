"use strict";
const meuEl = $(".elemento");
const difference = _.difference([1, 2, 3, 4, 5], [20, 3, 1, 11, 2]);
console.log(difference);
//  Ao utilizar sem lib sem types definidos deverei declarar a variável global no d.ts
